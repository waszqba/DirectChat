<UserControl x:Class="DirectChat.MsgTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DirectChat"
             mc:Ignorable="d" Height="40">
    <Grid>
        <Border Name="MainBorder" BorderThickness="0,0,0,1" BorderBrush="#FFEFEFEF" Padding="5,0">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Name="AddressBlock" FontSize="14"/>
                    <TextBlock Name="TimeBlock" Grid.Column="1" FontSize="10" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock Name="MsgBlock" FontSize="14" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
